apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-richie-antivirus.fullname" . }}
  labels:
    {{- include "atlas-richie-antivirus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "atlas-richie-antivirus.selectorLabels" . | nindent 4 }}
