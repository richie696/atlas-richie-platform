server:
  port: 9525

spring:
  application:
    name: sample-vector-postgresql

logging:
  level:
    root: info
    com.richie.component.vector.postgresql: debug

platform:
  component:
    vector:
      postgresql:
        url: jdbc:postgresql://localhost:5432/vectordb
        username: postgres
        password: password
        table: vectors
        dimension: 768
        metric: COSINE


