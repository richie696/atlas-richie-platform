server:
  port: 9527

spring:
  application:
    name: sample-vector-redis

logging:
  level:
    root: info
    com.richie.component.vector.redis: debug

platform:
  component:
    vector:
      redis:
        host: localhost
        port: 6379
        db: 0
        namespace: vectors
        dimension: 768
        metric: COSINE


